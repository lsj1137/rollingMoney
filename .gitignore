# ------------------------------------
# 1. IDE 및 OS 관련 파일 (Eclipse/VS Code/macOS/Windows)
# ------------------------------------
# Eclipse IDE files
.classpath
.project
.settings/
/bin/
/build/classes/
.metadata      # << 이클립스 워크스페이스 메타데이터 (필수 제외)
.metadata/*

# VS Code files (선택 사항, VS Code를 사용한다면 추가)
.vscode/

# 운영체제 파일
Thumbs.db       # Windows 썸네일 캐시
.DS_Store       # macOS 시스템 파일

# ------------------------------------
# 2. Node.js 및 Frontend Build 관련 파일 (Tailwind CSS)
# ------------------------------------
/node_modules   # << NPM 패키지 폴더 (필수 제외)
/npm-debug.log*
/yarn-debug.log*
/yarn-error.log*
/dist/          # 프로덕션 빌드 폴더 (만약 빌드 결과를 별도의 dist 폴더에 저장한다면)

# ------------------------------------
# 3. Java 및 WAS 관련 파일 (JSP/Servlet)
# ------------------------------------
# Class Files (컴파일된 파일)
# Eclipse의 경우 bin 폴더
/bin/

# Maven/Gradle 빌드 결과 (사용한다면)
/target/

# Log Files
*.log

# Deployment/War Files (배포용 파일)
*.war
*.ear
*.zip

# Local environment variables
.env
.env.local